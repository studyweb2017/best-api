{
  "name": "api-server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "date": "apk update && apk add tzdata && cp /usr/share/zoneinfo/Hongkong /etc/localtime",
    "proxy": "npm set registry https://registry.npm.taobao.org",
    "init": "npm i",
    "compose": "npm run date && npm run proxy && npm run init && sh",
    "dev": "",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yalishizhude",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.3.0",
    "koa-body-parser": "^1.1.2",
    "koa-router": "^7.2.1",
    "mongoose": "^4.11.0",
    "request": "^2.81.0",
    "babel-eslint": "^7.1.1"
  }
}
