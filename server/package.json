{
  "name": "api-server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "compose": "npm run proxy && npm run init && sh",
    "dev": "nodemon -L -e ts,json  --exec \"tsc &&node --harmony --inspect=0.0.0.0:5858 dist/app.js\"",
    "init": "npm i",
    "proxy": "npm set registry https://registry.npm.taobao.org",
    "start": "pm2 start ./dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yalishizhude",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.3.0",
    "koa-body-parser": "^1.1.2",
    "koa-router": "^7.2.1",
    "mongoose": "^4.11.0",
    "request": "^2.81.0",
    "babel-eslint": "^7.1.1"
  }
}
